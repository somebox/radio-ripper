require 'rubygems'
require 'bundler'

$:.unshift('./lib')

require 'radio-ripper'

# load 'tasks/*.rake'
Dir.glob('tasks/**/*.rake').each { |r| Rake.application.add_import r }

# task :default => :spec

